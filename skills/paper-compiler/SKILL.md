---
name: paper-compiler
description: 箇条書きのアイディア・研究メモ・議事録・文字起こしから論文ドラフトを「コンパイル」し、不足スロットを❌/⚠️で特定して充足質問を返す論文執筆・レビュー・監査の統合スキル（v2）。writeモード（素材→構造化ドラフト生成）、reviewモード（既存ドラフトのスロット検査・収束ループ）、auditモード（完成稿の投稿前18項目監査＝旧 paper-review-imrad を吸収）の3モードを持つ。「論文にして」「論文化」「ドラフト化」「paper_template」「論文コンパイル」「このメモから論文」「reviewモードで」「投稿に向けて足りないものを教えて」「実験計画のアドバイス」、および「論文を確認して」「check my paper」「draft review」「投稿前チェック」「IMRADチェック」といった依頼、または paper_template_vX.docx や論文PDF/原稿が添付されたときは必ずこのスキルを使う。提案形式（A アーキテクチャ提案型 / B アルゴリズム型 / C システム・アーティファクト型 / D 実証研究型）×研究フェーズ（P1〜P4）×投稿先 tier で骨格（IMRAD または 箱テンプレ5章）とチェックリストを切り替える。アブスト7文法（浦木・閉世界）を IMRAD の Abstract/Introduction 生成レシピと監査項目18として内蔵。Compile bullet-point research ideas, memos, and transcripts into a structured paper draft (IMRAD by default; Kiyoki-lineage 5-chapter "hako" template for EJC/IOS Press), flag missing slots with ❌/⚠️ and fulfillment questions, and audit finished manuscripts against an 18-item pre-submission checklist — write, review, and audit in one skill.
---

# paper-compiler v2 — 論文コンパイラ（write / review / audit 3モード）

## 概念

チェックリストを**状態機械**として使う論文コンパイラ。
`素材（箇条書き・メモ・文字起こし）→ スロット行列への写像 → 埋まらないスロットを❌検出 → ユーザーが充足質問に回答 → 再コンパイル` のループを回し、**すべての❌/⚠️を潰せば論文が完成する（チェックリスト＝十分条件）**ことを保証する。

このため、**❌/⚠️を出すときは必ず「充足質問」または「既存素材からの下書き案」を添える**。「何が欠けているか」だけの指摘は禁止。ユーザーに執筆労力を割かせないことが本スキルの目的である。

## モード判定（最初に必ず行う）

| 入力・トリガーの例 | モード | 参照 |
|---|---|---|
| 素材のみ／「論文にして」「このメモから論文」 | **write**：生成＋ギャップ検出 | 骨格のスロット行列＋`evaluation-planner.md` |
| ドラフトvXあり／「足りないものを教えて」「reviewモードで」 | **review**：スロット検査→充足→再コンパイル | 同上 |
| 完成稿（PDF/原稿）／「論文を確認して」「投稿前チェック」「IMRADチェック」 | **audit**：単発の18項目監査 | `paper-auditor.md` |

- review と audit の違い：review＝**収束ループ**（改稿前提・DOCX更新を伴う）、audit＝**単発合否ゲート**（投稿直前・その場で完結）。
- audit で❌が3件以上 or HIGH指摘がある場合は、review ループへの移行を提案する（`paper-auditor.md` 参照）。

## 応答冒頭の共通ヘッダー（write / review / audit すべてで必須）

**モードを問わず、チャットへの応答は必ず次の3ブロックのステータスから書き始める。** これは論文の状態を毎回一目で共有するための固定ヘッダーであり、モード固有の出力（ドラフト・レビュー表・監査表）はその後に続ける。省略しない。

```
## 📄 [タイトル or 手法名]  ｜ Mode: write / review / audit  ｜ vX

### 1. Initial Question Sheet（初期質問テーブル）
| 軸 | 値 |
|---|---|
| 型 | A / B / C / D（未確定なら推定＋⚠️） |
| フェーズ | P1 / P2 / P3 / P4 |
| venue（骨格） | 〔venue〕（IMRAD / 箱テンプレ） |
| 言語 | JA / EN |
| 手法名 | 〔固有名詞 or ⚠️未確定〕 |
| 中核主張 | 〔1文 or ❌未記入〕 |
（paper_template 添付時はその①から転記。未添付時は会話・素材から埋め、確定できない項目は ❌/⚠️ で示す）

### 2. Literature Review（関連研究）
- 状態：✅ N件（2020年以降 X%）／⚠️ 出典不明 M件／❌ 未着手
- （未着手・要更新なら literature-reviewer の呼び出しを提案。文献に変更があった場合は再生成）

### 3. Review History（レビュー履歴）
| vX（日時） | 要点（✅良い点／❌⚠️件数・前版比／HIGH／充足質問） |
|---|---|
| vX（当日時刻） | 〔今回の要約4項目〕 |
| …（前版があれば直近1〜2行を再掲） | … |
```

- **paper_template（DOCX）が添付されている場合**：3ブロックは DOCX の①②③から現在値を転記する（DOCX が正）。
- **DOCX が未添付の場合**（素材のみ・PDF貼り付け等）：チャット内で3ブロックを構築し、上記の見出しで暫定表示する。write ではこの後 paper_template_v0 の作成を提案し、review/audit では「DOCX があれば履歴が永続化される」旨を一度添える。
- **audit（単発監査）でも省略しない**：audit は DOCX を更新しないが、ヘッダーの3ブロックは「監査時点のスナップショット」として表示する（Review History には当該監査の行を1行示すのみで、DOCX への追記は行わない）。
- 3ブロックのいずれかが空でも見出しは必ず出し、`❌ 未記入`／`❌ 未着手` と明示する（見出しごと省略しない）。

## 入力の3軸

初回に paper_template_vX.docx の1ページ目（初期質問テーブル）から読み取る。未添付の場合は会話・素材から推定し、確定できない軸のみ質問する。

### 軸1：提案形式（型）

| 型 | 核となる貢献 | Methods/提案章の内部構造 |
|---|---|---|
| **A** アーキテクチャ提案型 | 概念モデル＋構成要素＋主関数 | 概念 → N要素分解 → Primitive Functions → 統合写像 |
| **B** アルゴリズム型 | 手続き（パイプライン） | ステップ列Overview → 各要素定義 → 入出力データ構造 → 統合関数 |
| **C** システム/アーティファクト型 | 動くシステム全体の設計判断 | 要求 → 設計判断と根拠 → アーキテクチャ → 実装 → 運用知見 |
| **D** 実証研究型 | 現象の観察・測定からの知見 | RQ/仮説 → 研究デザイン → 参加者/データ → 手続き → 分析手法 → 倫理 |

**A/B判定プロトコル（流し込みエディタ・浦木 由来）**：システム／概念モデル／構成要素が中心 → A、手順／ステップ／入力→計算→出力が中心 → B。推定した型を**出力の冒頭に明示**し、判断が割れる場合のみ一度だけ確認する。選んだ型のブロックだけを使い、**箱にない構成を足さない**（詳細は `paper-composition-checker.md`）。

### 軸2：研究フェーズ（＝評価の契約）

| フェーズ | 必要な証拠 | 許される主張 | 禁止される主張 |
|---|---|---|---|
| **P1** ポジション | 予備実験・概念実証・シナリオ | feasibility・方向性の妥当性 | 有効性・優位性の断定 |
| **P2** 実装 | 動作するシステム＋定性的ユーザー評価 | 実現可能性＋初期ユーザー知見 | 統計的一般化 |
| **P3** 提案 | 定量・統計的評価（ベースライン比較・検定・アブレーション） | 有効性・優位性 | — |
| **P4** 領域応用 | 定性＋定量の混合評価・ドメイン専門家の関与 | 実領域での有用性・転移可能性 | — |

### 軸3：投稿先（venue）

`国内研究会/WS` → `国際会議` → `トップ会議・ジャーナル` → `EJC・IOS Press系（清木系）` の4区分、または venue 名の直接指定。フェーズ×venue の実験・評価アドバイスは `evaluation-planner.md` を必ず読んで導出する。

## 骨格の選択ルール

1. **デフォルトは IMRAD**（`paper-blueprint-designer.md` を読む。アブスト7文法＝閉世界レシピを内蔵）。
2. **箱テンプレ5章構成**（アブスト/1背景/2提案/3実現/4実験/5まとめ）を使うのは、**型が A または B、かつ venue が EJC・IOS Press系（清木リネージ）**のとき、または明示指定があったときのみ（`paper-composition-checker.md` と `paper-composer.md` を読む）。
3. 型 C・D は常に IMRAD。骨格を混在させない。
4. 骨格が違ってもスロット行列は共通語彙（何を書くべきかの単位）であり、write/review は同一機構の裏表として動かす。

## writeモードの実行手順

0. **共通ヘッダーを出力**：まず「応答冒頭の共通ヘッダー」の3ブロック（Initial Question Sheet／Literature Review／Review History）を表示してから、以下に進む。
1. **3軸＋モードの確定**：テンプレ1ページ目 or 会話から読み取る。骨格（と箱テンプレの場合は採用バージョンA/B）を宣言する。
2. **対応する参照ファイルを読む**：骨格のスロット行列＋ `evaluation-planner.md`。
3. **素材→スロット写像表の提示**：添付されたメモ・文字起こしの各項目が、どのスロットに入るかの対応表を先に示す（ユーザーが写像の誤りを訂正できるようにする）。
4. **スロット生成**：埋められるスロットは、骨格の形式文体（IMRADならアブスト7文法、箱テンプレなら定型句・signature動詞・"X indicates"構文まで）で**全文を生成**する。プレースホルダは〔　〕で示す。
5. **ギャップレポート**：合格したスロットは✅として明示する（良くできている点を必ず言語化する）。埋められないスロットを ❌、部分的なものを ⚠️ とし、**各行に充足質問（ユーザーが答えれば✅になる具体的な質問）または下書き案**を添える。優先度 HIGH（投稿の成立に必須）/ MEDIUM / LOW を付す。
6. **実験・評価アドバイス**：`evaluation-planner.md` の「型×フェーズ×venue」から、必要な実験セット・評価指標・サンプル規模・倫理記述の要否を具体的に提示する。
7. **DOCX出力**：下記「テンプレートDOCXの運用」に従い vX を繰り上げ、**変更した全箇所に「更新箇所には必ずコメントを付す」規定どおりWordコメント（[vX 更新] 何を・なぜ）を付与し**、**「レビュー履歴」テーブルの最上段に当該版の行を箇条書き4項目（✅／件数／HIGH／充足質問）で追記**して出力する。本文への個別の修正提案・充足質問は「本文への修正提案」の規定どおりWordコメント・変更履歴として該当箇所に付与する。

### 要入力スロットの網掛け規定（旧・色分けHTMLの後継）

- 埋められない〔　〕スロット（❌/⚠️付き）には、DOCX内で**黄色の文字網掛け（`w:highlight="yellow"`）**を付け、充足質問をコメントとしてアンカーする。ドラフトを開いた瞬間に「あとどこを埋めれば完成か」が視覚的に分かる状態を保つ。
- スロットが充足されたら網掛けを解除する。**投稿版（submission / camera-ready）では黄色網掛けが残っていないことを確認**する（残存＝未充足スロットの検出にも使える）。
- 旧・流し込みエディタの色分けHTML出力（グレー＝箱／青＝アイデア由来）は廃止。定型部分の識別は網掛けではなく `paper-composer.md`（原本）との照合で行う。

## reviewモードの実行手順

1. 3軸を確定し、対応するスロット行列をルーブリックとしてドラフト全文に適用する。
2. 判定記号は全モード共通：

| 記号 | 意味 |
|---|---|
| ✅ | 合格（本文に明確な根拠） |
| ❌ | 欠落（スロットが空） |
| ⚠️ | 部分的・要確認 |
| N/A | 型・フェーズ・venue上対象外 |

3. 出力フォーマット（**冒頭に必ず「応答冒頭の共通ヘッダー」の3ブロックを置き、その下に**以下を続ける）：

```
（↑ Initial Question Sheet / Literature Review / Review History の3ブロックを先に出力）

## 📋 paper-compiler レビュー結果
**タイトル**／**型・フェーズ・venue・骨格**／**版**：vX

| # | スロット | 判定 | 根拠（本文引用） | 充足質問 / 下書き案 |
|---|---|---|---|---|
（骨格別スロット行列の全項目。❌/⚠️の行は充足質問欄を必ず埋める）

## 🎯 収束レポート
- ✅ ハイライト：今回よくできている点・前版から✅に転じた項目（必ず冒頭に置く）
- 残り❌：N件 / ⚠️：M件（前版比）
- 優先度 HIGH / MEDIUM / LOW 別の次アクション
- 実験・評価に関するアドバイス（フェーズ×venue由来）
```

4. **レビュー履歴への記録**：レビュー結果の要約を、DOCXの「レビュー履歴」テーブルの最上段に当該版の行として**箇条書き4項目（✅／件数／HIGH／充足質問）**で追記する（詳細は「テンプレートDOCXの運用」参照）。本文を変更した場合は、**変更箇所すべてに「更新箇所には必ずコメントを付す」規定どおりコメントを付与**する。
5. **収束の定義**：❌=0 かつ HIGH の⚠️=0 になったら「投稿可能版」到達を宣言し、投稿版の整形（初期質問テーブル・レビュー履歴ページの削除・黄色網掛けゼロの確認・venueテンプレへの流し込み）を提案する。

## auditモードの実行手順

0. **共通ヘッダーを出力**：まず「応答冒頭の共通ヘッダー」の3ブロックを（監査時点のスナップショットとして）表示する。Review History は当該監査の1行のみとし、DOCX への追記は行わない。
1. `paper-auditor.md` を必ず読み、その手順・18項目・出力フォーマットに従って単発監査を行う（項目18＝アブスト7文法・閉世界チェック）。監査表は共通ヘッダーの後に続ける。
2. 骨格が箱テンプレ5章の場合、IMRAD専用項目（09・15）は N/A とし、`paper-composition-checker.md` の H-X 整合チェックの実施を提案する。
3. 監査は単発で完結する（DOCX更新・レビュー履歴追記は行わない）。**❌が3件以上 or HIGH指摘あり**の場合のみ、reviewモードへの移行を提案する。

## 本文への修正提案：Wordコメント＋変更履歴で行う（インライン注記の禁止）

ドラフト本文に対する個別の修正提案は、**本文中に〔コメント：…〕のような注記を書き込まず**、DOCXのネイティブ機能で該当箇所にアンカーする。docx スキルの手順に従う：

- **著者名の規定（コメント・変更履歴の名義）：2名を使い分ける。** `"Claude"` やスキル名（`"paper-compiler"` 等）は絶対に使わない。

  | 名義 | イニシャル | 使う対象 |
  |---|---|---|
  | **`Yusuke Takahashi, PhD`** | `YT` | **既定（デフォルト）**。下記以外のすべてのコメント・変更履歴 |
  | **`Asako Uraki, PhD`** | `AU` | **浦木メソッド由来の指摘のみ**（下記の判定基準に該当するもの） |

  **`Asako Uraki, PhD` 名義にする指摘（浦木先生の貢献部分）**：
  1. **アブストラクト・1章の7文法（閉世界）**に関する指摘 — 7文の順序・欠落・前半4文で閉世界が成立しているか。audit **項目18**（閉世界チェック）に紐づくコメントを含む。`paper-blueprint-designer.md` の「アブスト7文法」節に根拠を持つ指摘。
  2. **箱テンプレート5章骨格**に関する指摘 — 定型句・signature動詞（propose / present / define / realize / switch / adapt / apply）・自己引用ブロック・アブスト①〜⑥の構成・`"X indicates ..."` 構文・5章の着地など、`paper-composer.md`（原本）に根拠を持つ指摘。
  3. **章間整合 H-X01〜18** に関する指摘 — 手法名の一字一句一致、2章の記号系と3章の実装の一致、アブスト⑤の貢献リストと5章の貢献リストの一致など、`paper-composition-checker.md` に根拠を持つ指摘。
  4. **A/B判定プロトコル**（流し込みエディタ由来）に関する指摘 — 採用バージョンの明示、箱にない構成を足していないか。

  上記に該当しない指摘（IMRAD一般の規律、参考文献の7割2020年ルール、Results に解釈を混ぜない、実験・評価計画、投稿先適合、岡先生フィードバック由来の「トップ誌の流儀」など）は、すべて **`Yusuke Takahashi, PhD`** 名義とする。

  - **判定に迷う場合は `Yusuke Takahashi, PhD`（既定）**を使う。浦木名義は「根拠が浦木メソッドにあると明確に言える指摘」に限定する。
  - **コメント本文の冒頭に根拠を明示**すると読み手に親切（例：`[7文法] 1文目が技術から始まっています。人間の機能から書き起こしてください（閉世界の前半4文）。`）。
  - 実装：コメントごとに著者を切り替える。`comment.py --author "Asako Uraki, PhD" --initials "AU"` と `comment.py --author "Yusuke Takahashi, PhD" --initials "YT"` を、指摘の種類に応じて使い分けて comments.xml を生成する（1つの文書に両名義のコメントが混在してよい）。
  - **変更履歴（tracked changes）は常に `w:author="Yusuke Takahashi, PhD"`** とする（本文の書き換えは執筆側の操作であり、浦木名義は「指摘＝コメント」に限定する）。
- **語句・文の書き換え提案** → **変更履歴（tracked changes）**：`<w:ins>`（挿入）／`<w:del>`（削除、テキスト要素は `<w:delText>`）で `w:author="Yusuke Takahashi, PhD"`・`w:date` を付けて記録する。ユーザーがWord上で承認/却下できる形にする。書き換えの**理由**が浦木メソッド由来であれば、その箇所のコメントは `Asako Uraki, PhD` 名義で添える。
- **理由説明・充足質問・代替案の提示** → **コメント機能**：docx スキルの `scripts/comment.py` で comments.xml 群を生成し、`<w:commentRangeStart/End>`・`<w:commentReference>` を該当テキストに配置してアンカーする。特定箇所に紐づく充足質問はギャップレポートだけでなく、その箇所へのコメントとしても付与する。
- **スロット自体が欠落している場合**のみ、本文のあるべき位置に〔　〕プレースホルダ（❌マーク＋黄色網掛け付き）を置き、そこにコメントで充足質問をアンカーする。
- レビュー履歴テーブルには要約のみを書き、個別提案の本文はコメント/変更履歴側に置く（二重管理しない）。
- 出力前に `validate.py --original` で検証する（tracked changes を含むため必須）。

### 更新箇所には必ずコメントを付す（改稿のたびに必須）

**vX → vX+1 で本文を変更（新規生成・加筆・書き換え・削除）したすべての箇所に、Wordコメントで「何を・なぜ変えたか」を残す。** ユーザーが差分を追えないまま新版を受け取ることがないようにするための必須規定であり、省略は違反。

- **付与単位**：変更した段落・文・表セルごとに1コメント。連続する小さな変更は1つにまとめてよいが、**章をまたいで1つにまとめない**。
- **コメント本文の書式**（1〜3行、平易な日本語／論文がENなら英語）：

  ```
  [v2 更新] 何を変えたか（1行）
  理由：なぜ変えたか（スロットID・監査項目・充足質問の回答など根拠を明示）
  ※ 必要なら：他章への波及（例：手法名を変更したため2章・5章も同期）
  ```

  例：
  - `[v2 更新] Abstract 1文目を「人間の粒度切替」から書き起こす形に変更。理由：7文法の1文目（人間の機能）で閉世界を開始するため（audit項目18）。`
  - `[v3 更新] Results から「〜と考えられる」を削除し Discussion へ移動。理由：Results に解釈を混ぜない（audit項目15／スロットE）。`
  - `[v2 更新] 実験結果の数値を追記（92.4% / ベースライン85.1%）。理由：充足質問「代表的な結果数値」へのご回答を反映。❌→✅。`

- **変更の種類ごとの扱い**：
  - 語句・文の**書き換え／削除** → 変更履歴（tracked changes）で記録し、**その箇所にコメントで理由を添える**（変更履歴だけで理由を書かない）。
  - **新規生成したスロット**（前版で空だった章・節を埋めた） → 生成範囲の冒頭にコメントで `[vX 更新] 新規生成` と根拠（どの素材・どの回答から書いたか）を明示する。
  - **充足された❌/⚠️** → 該当箇所のコメントに `❌→✅`（または `⚠️→✅`）と、どの充足質問の回答を反映したかを書く。**黄色網掛けの解除も同時に行う**。
  - **他章への波及同期**（手法名・記号系・貢献リスト等） → 波及先の各箇所にも `[vX 更新] 〈手法名〉変更に伴う同期（H-X01）` のようにコメントを付す。
- **前版のコメントは削除しない**（履歴として残す）。解決済みのものは、可能なら解決マークを付ける。
- 著者名は上記「著者名の規定」に従う：既定は `Yusuke Takahashi, PhD`（`YT`）、**浦木メソッド由来の指摘（7文法・箱テンプレ・H-X整合・A/B判定）のコメントのみ `Asako Uraki, PhD`（`AU`）**。変更履歴は常に `Yusuke Takahashi, PhD`。
- コメントの総数と主な更新点は、レビュー履歴テーブルの当該版の行にも1行で要約する（詳細は各コメント側に置き、二重管理はしない）。

## テンプレートDOCXの運用（paper_template）

- 雛形は `assets/paper_template_v0.docx`。運用時は次の**4部構成**とする：**①初期質問テーブル**（3軸・タイトル・手法名・中核主張・著者・締切・素材一覧）→ 改ページ →**②Literature Review（関連研究）**（`literature-reviewer` スキルが生成・更新。「初期質問テーブル」と「レビュー履歴」の間に改ページで挿入する）→ 改ページ →**③「レビュー履歴」テーブル**（2列：`vX（日時）｜レビュー内容`）→ 改ページ →**④論文ドラフト領域**。
- **literature-reviewer の呼び出し**：write の初回コンパイル時（および文献に変更があった改稿時）に literature-reviewer スキルを呼び出し、②を生成・上書き更新する。各文献は「書誌／中学生でもわかる数行の概要／本文URL」の3点セット。URL未確認は ⚠️ 出典不明・要確認。投稿版では ✅ の文献を本文 References へ転記して②を削除し、⚠️ が残る文献は転記しない。
- ユーザーは1ページ目を記入した paper_template と、議事録・文字起こし・メモを添付して本スキルを起動する。
- **改稿のたびに vX を+1**し、**初期質問テーブルとレビュー履歴は投稿版まで常に維持**する。ドラフトはドラフト領域を書き換える。
- **レビュー履歴の記録ルール**：コンパイル/レビューを行うたびに、**ヘッダー直下（最上段）に新しい版の行を追加**する（v2の行がv1の行の上に来る＝新しい版ほど上）。`vX（日時）`列には版番号と実施日時を記載する。`レビュー内容`列は**1行の連続文にせず、必ず次の4項目の箇条書き**で書く：
  1. ✅ 良くできている点・合格したスロット（**うまくできている点は必ず✅として明示的に褒める**。前版から改善した項目も含む）
  2. ❌／⚠️ の件数（前版比の増減付き）
  3. 優先度HIGHの指摘
  4. 主要な充足質問
  既存の行は編集せず履歴として保存する。
- **投稿版（submission / camera-ready）では初期質問テーブル・Literature Review・「レビュー履歴」の各ページを削除**する（Literature Review は転記後に削除）。
- **ファイル命名規則**（file-naming-convention 準拠・本スキル専用の拡張形）：

  ```
  {YYYYMMDD}_#{VENUE}_{SHORT_TITLE}_@{FIRST_AUTHOR}_{LANG}_v{X}.docx
  ```

  例：`20260706_#EJC2027_TimeAxisSynth_@Takahashi_JA_v0.docx`
  - `YYYYMMDD`＝最新版を出力した当日の日付（改稿ごとに更新）
  - `#{VENUE}`＝投稿先（Q4 から。例：`#EJC2027` `#iCAST2026` `#CSCW2027`。未定なら `#TBD`）
  - `SHORT_TITLE`＝短縮タイトル（英数字・アンダースコア、空白なし）
  - `@{FIRST_AUTHOR}`＝筆頭著者の姓（例：`@Takahashi`）
  - `LANG`＝`JA`/`EN`（対訳セットは日付・版を揃え、言語のみ変える）
  - `v0`＝質問シート記入済み・初回コンパイル前。以降、改稿ごとに +1
  - 雛形自体の名称 `paper_template_v0.docx`（Skill内アセット）は例外的に固定。プロジェクト開始時に複製し、上記パターンに改名して使う。
- DOCX の生成・編集は docx スキルの手順（新規=docx-js／既存編集=unpack→XML→pack→validate）に従う。

## 参照ファイル（役割名で統一）

| ファイル | 役割 | いつ読むか |
|---|---|---|
| `references/paper-blueprint-designer.md` | **設計図**：IMRAD骨格スロット行列（型A〜D別Methods＋アブスト7文法レシピ内蔵） | 骨格=IMRAD のとき必読 |
| `references/paper-composer.md` | **組版工**：箱テンプレ原本（浦木・定型句と文体申し送り。改変禁止） | 箱テンプレの定型句・文体でスロットを生成するとき |
| `references/paper-composition-checker.md` | **組版検査**：箱テンプレのスロットID・章間整合H-X01〜18・A/B判定プロトコル | 骨格=箱テンプレのとき必読 |
| `references/paper-auditor.md` | **監査官**：投稿前18項目の単発合否ゲート（旧 paper-review-imrad） | auditモードで必読 |
| `references/evaluation-planner.md` | **評価計画**：型×フェーズ×venue の実験・評価要求水準 | write/review 双方で実験・評価アドバイスを出すとき必読 |
| `assets/paper_template_v0.docx` | 論文テンプレ雛形 | 新規プロジェクト開始時にユーザーへ渡す |

## 他スキルとの連携・振り分け

- **paper-review-imrad（旧・JA/EN）**：本スキル v2 の auditモードに**吸収済み**。旧スキルが同一環境に登録されている場合は起動が競合するため、**削除を推奨**する。
- **literature-reviewer**：参考文献の調査・平易な要約・URL検証を担う独立スキル。write モードから呼び出し、paper_template の「初期質問テーブル」と「レビュー履歴」の間に改ページで Literature Review（関連研究）を挿入・更新させる。audit で項目08〜11に⚠️が出た場合も呼び出しを提案する。
- **research-starter**：最上流（研究開始の環境整備）。全Phase完了後に本スキルへ接続される。
- **research-proposer**：上流（複数論文のポートフォリオ・投稿戦略・プロジェクト計画書）。本スキルは単一論文のコンパイルを担当。
- **docx / file-naming-convention**：出力時に必ず適用。
- **skill-creator**：本スキルの評価・洗練（学生論文＝既知欠陥の検出率、優良採択論文＝偽❌率の2指標）を行うときに使用。

## 重要な注意

- **改稿で本文を変更したのに、変更箇所へWordコメント（[vX 更新] 何を・なぜ）を付けないのは違反。** 差分が追えない新版を渡さない（「更新箇所には必ずコメントを付す」参照）。
- **応答冒頭の共通ヘッダー（Initial Question Sheet／Literature Review／Review History の3ブロック）を省略するのは違反。** write / review / audit のいずれのモードでも、モード固有の出力の前に必ず3ブロックを表示する。空の項目は見出しごと省かず `❌ 未記入`／`❌ 未着手` と明示する。
- ❌/⚠️に充足質問も下書き案も添えない指摘は本スキルの違反。同様に、✅（良くできている点）を一切挙げないレビューも違反。
- 本文への修正提案をインライン注記（〔コメント：…〕等）で書き込むのは違反。必ずWordコメント・変更履歴を使う。
- フェーズが許さない主張（例：P1でeffectivenessの断定）を生成しない。reviewでは過剰主張を⚠️として検出する。
- 箱テンプレ骨格に IMRAD 基準（例：参考文献7割2020年以降、Resultsに解釈禁止、アブスト7文法レシピ）を適用しない。逆も同じ。骨格ごとの基準は各スロット行列に従う。閉世界の検査観点のみ両骨格で共有する。
- 研究内容そのものの真正性（データの実在・統計の再計算）は保証しない。判定は「スロットが規定どおり書かれているか」に限る。
